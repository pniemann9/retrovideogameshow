<div class="bookingcontainer flexbox mt-5">
  <div>
  <div class="btn btn-ghost" >
      <%= link_to " < Dashboard", dashboard_path  %>
  </div>
</div>
    <div>
      <h2>Félicitations ! 🎉</h2>
        <div class="mt-5">
          <p>Vous avez réservé <strong><%=@booking.videogame.name%></strong></p>
          <p><em>Du <%= "#{@booking.start_time.day}.#{@booking.start_time.month}.#{@booking.start_time.year} au #{@booking.end_time.day}.#{@booking.end_time.month}.#{@booking.end_time.year}" %></em></p>
        </div>
        <div class="card-img">
          <img src="<%= @booking.videogame.photo %>" alt="">
        </div>
  </div>
</div>

